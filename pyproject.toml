[project]
name = "reflex-alpha"
version = "0.1.0"
description = "Backend API for Reflex Alpha adaptive trading simulator"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = false
